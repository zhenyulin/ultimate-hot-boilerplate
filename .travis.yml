sudo: required
language: node_js
services:
  - docker
node_js:
  - "node"
script:
  - docker build . -t test
  - docker run test:latest test
  - yarn cover
after_script:
  - cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js
branches:
  only:
    - master
cache:
  yarn: true
  directories:
  - node_modules
